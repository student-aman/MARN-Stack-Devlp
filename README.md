if your run the website on the browser that this packages to downlaod :

#this line to copy and then you terminal in paste:
npm install express mongoose dotenv ejs bcryptjs jsonwebtoken cookie-parser

#the nodemon package to downlaod:
npm install --save-dev nodemon
